import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hafele.india',
  appName: 'hafeleindia',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
